<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Точные будильники: USE_EXACT_ALARM авто-выдаётся приложениям-будильникам (API 33+),
         SCHEDULE_EXACT_ALARM — для более старых. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <!-- Обновление производственного календаря с офиц. источника (isDayOff.ru). Не критично для
         звонка: движок читает локальный кэш/встроенные данные, сеть — фоновое обновление. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ShiftAlarmApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/backup_rules"
        android:fullBackupContent="@xml/full_backup_content"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShiftAlarm">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- «Показать будильники»: приложение можно назначить обработчиком (виджеты
                 часов, Ассистент). Ближайший будильник уже открывается через setAlarmClock. -->
            <intent-filter>
                <action android:name="android.intent.action.SHOW_ALARMS" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Обработчик «поставить будильник» (Ассистент/сторонние приложения). Без своего UI —
             создаёт будильник и закрывается, поэтому прозрачная тема. -->
        <activity
            android:name=".SetAlarmActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.SET_ALARM" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Полноэкранный экран звонка: поверх блокировки, будит экран. directBootAware: должен
             показываться и до разблокировки (звонок на локскрине после ночного ребута). -->
        <activity
            android:name=".AlarmActivity"
            android:exported="false"
            android:directBootAware="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:excludeFromRecents="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.ShiftAlarm" />

        <!-- Ловит срабатывание AlarmManager. directBootAware: должен отработать и до разблокировки —
             тогда звонит по данным из RingCache/extras, не трогая Room. -->
        <receiver
            android:name=".alarm.AlarmReceiver"
            android:exported="false"
            android:directBootAware="true" />

        <!-- Проигрывает сигнал независимо от UI. directBootAware: играет и до разблокировки. -->
        <service
            android:name=".alarm.AlarmService"
            android:exported="false"
            android:directBootAware="true"
            android:foregroundServiceType="mediaPlayback" />

        <!-- Перерегистрация будильников после системных событий. directBootAware: на LOCKED_BOOT_COMPLETED
             (ещё до ввода пин-кода) перевыставляет из device-protected кэша, не трогая Room. -->
        <receiver
            android:name=".alarm.BootReceiver"
            android:exported="true"
            android:directBootAware="true">
            <intent-filter>
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.USER_UNLOCKED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.app.action.SCHEDULE_EXACT_ALARM_PERMISSION_STATE_CHANGED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
